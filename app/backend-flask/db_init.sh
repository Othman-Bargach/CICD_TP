# WIP : shell script to initiate a postgres instance and create and populate the necessary tables
# This script will likely be dropped, and isn't currently of any use

# docker pull postgres:latest
# docker run --name pokesql -e POSTGRES_USER=root -e POSTGRES_PASSWORD=gottacatchemall -p 5432:5432 -d postgres
# docker exec -it pokesql psql -U root
